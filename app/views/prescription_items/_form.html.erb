<%= form_with(model: prescription_item, local: true) do |form| %>
  <% if prescription_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prescription_item.errors.count, "error") %> prohibited this prescription_item from being saved:</h2>

      <ul>
      <% prescription_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :prescription_id %>
    <%= form.text_field :prescription_id %>
  </div>

  <div class="field">
    <%= form.label :medicament_id %>
    <%= form.text_field :medicament_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
