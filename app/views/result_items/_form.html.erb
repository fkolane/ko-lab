<%= form_with(model: result_item, local: true) do |form| %>
  <% if result_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(result_item.errors.count, "error") %> prohibited this result_item from being saved:</h2>

      <ul>
      <% result_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
